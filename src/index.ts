/**
 * Return false if the argument is not a number
 * @param num
 */
export function addTwo (num: number): number {
    return num + 2
}

/**
 * Return false if the argument is not a number
 * @param num
 */
 export function addTree (num: number): number {
    return num + 3
}
